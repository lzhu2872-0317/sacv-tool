# SACV-Tool 1.2.3

- Benchmark CSV 现在同时接受 `citation`、`citation_text`、`reference` 或 `raw_citation` 作为引用正文列。
- `SACV-Benchmark-1000.csv` 无需重命名列即可直接传给 `sacv benchmark`。
- 增加完整数据集 schema 回归测试；自动化测试总数为 45。

# Recommended for Crossref's polite pool and required for responsible PubMed use.
SACV_EMAIL=your.name@um.edu.my
# Optional; only needed for PubMed rates above 3 requests/second.
NCBI_API_KEY=
# Optional; OpenAlex works without a key at the normal public rate.
OPENALEX_API_KEY=
